# Development Dockerfile
FROM node:20-alpine

WORKDIR /app

# Set npm to use strict-ssl=false for corporate proxy/firewall
RUN npm config set strict-ssl false

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source files
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
